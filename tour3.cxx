#include <iostream>

enum class Colour {red, orange, yellow, blue};
enum class TrafficLight {red, amber, green};

int main()
{
    Colour l = Colour::red;
    TrafficLight t = TrafficLight::red;
    
    return 0;
}